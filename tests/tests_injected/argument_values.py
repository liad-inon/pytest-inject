"""
    Constants representing different states for tests arguments
    to check injection effect.
    Used as an expected argument value interface between the injected
    tests and injecting tests.
"""
INJECTED = "INJECTED"
NOT_EFFECTED = "NOT_EFFECTED"
